# server {
#     listen 80;
#     server_name silkroadlodge.com www.silkroadlodge.com;

#     root /usr/share/nginx/html;
#     index index.html;

#     # 1. تشغيل واجهة الـ React
#     location / {
#         try_files $uri $uri/ /index.html;
#     }

#     # 2. تمرير طلبات الـ API إلى حاوية الـ backend
#     location /api {
#         proxy_pass http://backend:8000; 
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

server {
    listen 80;
    server_name silkroadlodge.com www.silkroadlodge.com;

    root /usr/share/nginx/html;
    index index.html;

    # 1. تشغيل واجهة الـ React
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 2. التوجيه الأساسي لطلبات الـ API إلى ملف index.php التابع للارافيل
    location /api {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # 3. معالجة طلبات PHP عبر حاوية الباك إند بنظام FPM والمنفذ 9000
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass backend:9000; # 👈 الاتصال بالمنفذ الصحيح للحاوية
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/public$fastcgi_script_name; # مسار مجلد public داخل الحاوية
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}